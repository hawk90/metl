# Build directories
build/
build-*/
cmake-build-*/
out/
dist/

# CMake artifacts
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
install_manifest.txt
compile_commands.json

# IDE / editor / agent
.vs/
.vscode/
.idea/
.claude/
*.user
*.suo
*.swp
*~

# OS
.DS_Store

# Compiled objects and binaries
*.o
*.obj
*.a
*.lib
*.so
*.dll
*.dylib
*.exe

# Core dumps (anchored to the repo root so it does not swallow tests/core/)
/core
core.*

# libFuzzer crash / leak reproducers (do not commit; seeds live in fuzz/corpus/)
crash-*
leak-*
timeout-*
oom-*

# Third-party (placeholder)
/third_party

# clangd index cache
.cache/

# Python bytecode (tools/)
__pycache__/
*.pyc

# Conan generates this in the consumer folder and it holds absolute local paths.
CMakeUserPresets.json
