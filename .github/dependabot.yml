# Keep GitHub Actions up to date automatically — prevents Node-runtime
# deprecation drift (e.g. actions pinned to an old Node major).
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns: ["*"]
